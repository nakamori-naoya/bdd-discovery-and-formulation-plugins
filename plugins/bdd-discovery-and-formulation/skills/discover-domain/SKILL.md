---
name: discover-domain
description: コアドメインの業務知識と代表的な振る舞いを共通理解にし、BDDを含むdomain-rule資料を1本作る。業務イベント、事前状態、アクター、業務の行い（誰が行えるか、成り立つ条件、拒む理由）、判断、次状態を整理する。「業務知識をBDD付きで整理して」「コアドメインを発見して」と言われたとき、実装やQA反証へ進む前に使う。
---

# コアドメインの共通理解をdomain-rule資料にする

読み終えると、利用者が持つ業務知識から、作りを全部やめても残る決まりだけを、業務の言葉で書いたdomain-rule資料1本へ保存できる。この資料は業務の人と開発者が同じ判断を説明するためのものであり、ユースケースの処理順、画面・操作導線、API・メッセージ契約、データモデル・永続化、コード構造、運用・非機能の実現方式、テスト実行は別の資料が担う。探求が先で、記述は後である。何が起きるのかを知らないまま型を埋めると、型の穴を埋める作り話が入る。

同じdirectoryの`playbook.yml`が工程順の定義である。このSKILLを読んだagentが、利用者の入力と明示された資料を保持したまま`steps`を宣言順に辿り、最後まで同じ文脈で判断する。`agent_work: invoking_agent`の工程はこのagentの認知工程、`skill:`の工程は同じpackageに同梱された同名skillの`SKILL.md`をこのagentが同じ文脈で読んで適用する工程、`script:`の工程は明示した入力から閉じた結果を返す決定論的tool、`playbook:`の工程は外部公開Skillの直接呼び出しである。工程間の値はこのagentが文脈に保持し、fileへ書き出して受け渡さない。手順に入る前に同梱の内部skill `write-bdd`の`SKILL.md`を同じ文脈で読み、その規律（入力の根拠づけ、業務の言葉、`grill` / `write-doc`の呼び方、BDDの前提・トリガー・失敗理由と条件マトリクス）を全工程へ適用する。

## 入力

| 入力 | 内容 | 満たさないときの扱い |
|---|---|---|
| `user_input` | 依頼文。題材と、利用者がすでに知っていること | 題材が読めなければ`settle`の問いにする。題材そのものが決まらなければ止まる |
| `references` | 任意。追加で従う資料の絶対path配列。手順の最初に読む。プロジェクト固有の規約や文脈は、対象repositoryのAGENTS.md / CLAUDE.mdとこの入力で渡される | 相対path、読めないpath、symlinkは公開契約に反する入力として止まり、正しいpathを求める |
| `output_directory` | 資料を置く既存の書き込み可能な絶対directory | 下の「保存先の決め方」に従う |
| `name` | path要素を含まない`.md`file名。日本語名を許す | 同上 |

**保存先の決め方。** 依頼に資料構成が示されていれば、そのまま使う。示されていなければ、利用者の既存資料のdirectory階層と命名を読み、置き場と名前を提案として`settle`の問いの1つに含めて確かめる。資料の置き場は利用者の資料構成に属するので、仮置きで書かない。既定のpathを持たない。`output_directory`が存在しないか相対pathなら止まって利用者へ返す。出力先と同じpathのfileがすでにある場合は上書きせず、未使用の名前を`settle`で提案して確かめる。同じpathへ新規保存できないことを、既存資料があるため探索できないことや、formulationへ切り替える理由にしない。未使用の保存先を確認できれば、既存資料を根拠として探索を続ける。

`write-bdd`の入力に根拠づける規律に従い、利用者の発言、明示された資料、`settle`で確認した決定にない業務用語、業務イベント、役割、決まりを事実として作らない。受け取った事実は、誰が確かめたのかで確からしさが変わるので、確認済み・仮説・未確認の区別を保ったまま扱う。

## 判断基準

### それは業務の話か

仕組みを全部やめても残り、業務を知る人がその言葉を使い、破られて困るのが業務である。この三つが成り立つ記述だけを業務の話として残す。どれかが成り立たなければ実装の関心として外し、元の記述、外した理由、送り先を`implementation_excluded`へ残す。ユースケースの手順、画面、API、データモデル、コード、運用、テストの具体がこれに当たる。迷ったら業務の話ではない側へ倒す。取りこぼしは次に業務を語れば戻るが、混入した実装の話は資料を腐らせる。

### 既存の資料は発見の根拠か、反証の対象か

同種の既存資料があっても、未知の業務イベント、役割、判断、境界を探す依頼なら、その資料を入力の根拠として読み、発見を続ける。記載済みというだけで確認済みにはしない。指定された既存のdomain-rule資料を反証して同じpathへ戻す依頼だけが、定式化（formulate-domain）の仕事である。

### コアはどこか

業務の話に残ったものを、コア・支援・汎用に分け、選んだ理由を一文で書く。代表BDDを書くのはコアだけである。理由が言えなければ、最も筋の良い区分を仮説として置き、根拠と採らなかった区分を未決に書く。

### 語と業務イベントは業務の言葉か

ユビキタス言語に載せるのは、業務の人が仕事の会話と判断で合意して使う語だけである。部品名、保存方式、内部の状態名、技術語を平易に言い換えただけの語は載せない。合意語が無ければ入力にある語を仮置きし、回答責任者つきの未決にする。業務イベントは過去形の業務の事実として書き、「〜する」「〜してほしい」の要求や画面の操作を業務イベントにしない。業務用語、業務イベント、業務の行いには、実装が使う英名を業務の言葉から付けて、ユビキタス言語の節に対応を置く（`write-bdd`のユビキタス言語の規律「英名も、業務の言葉から付ける」）。

### 業務の行いに、誰が行えるかと拒む理由がそろっているか

コマンドとクエリのそれぞれについて、誰が行えるか、それ以外の人が行うと誰が何に困るか、成り立つ条件、拒む理由を、その行いの節の一か所に書く。誰が行えるかは成り立つ条件と同じ重さの決まりである。どの仕組みが確かめるか（認証、認可、画面の制御）は書かない。判定は「破られたときに困るのは業務か運用か」で、運用の関心（トークンの形式と有効期限、利用回数の制限、管理者の操作）は書かない。期限の到来や別の出来事で起きる行いには、業務上判断する者の名前を書く。決まっていない行いは、最も筋の良い役割を仮説として置き、未決に残す。

### 代表BDDとして書くか

典型、既知の別の経路、既知の拒否を代表BDDにする。境界値、同値分割、順序の逆転、重複、同時実行による体系的な反証は、共通理解を作った後の定式化が担う。仮説の前提が変わると判断や結果が変わる振る舞いは、BDDに書くが、仮説であることと根拠をその箇所と未決に明示する。

### 資料にしてよいか

事実、線引き、決定、振る舞い断面、代表BDD、すべての業務の行いの節がそろったら資料にする。どれかが欠けていれば、そろうまで工程を戻す。

## 手順

1. **settle（`grill`）。** [実行指示書](references/execution-guidance.md)の「問いの選び方」で、答えによって資料の骨格（コアの範囲、業務イベント、判断、誰が行えるか）が変わる問いを成果を左右する順に選び、それぞれに推奨と理由を添えて`context`と`questions`に渡す。保存先が依頼に無ければ、その提案を問いに含める。呼び方は`write-bdd`の入れ子の段取りを呼ぶ規律に従う。問う数の上限と対話の作法はgrillの公開契約に従い、返った`open_questions`と、上限で問えなかった論点は、停止条件の判断基準で扱う。返った`decisions`と`open_questions`を利用者入力・明示資料と突き合わせる。2回目の`grill`は、利用者が求めた場合か、決定なしでは資料を完成できない場合だけ行う。
2. **ground。** 依頼、参照資料、決定、未決、停止条件で仮説として置いた推奨を根拠・仮説・未確認へ区別して`grounded_input`として保持する。
3. **explore。** 同梱skillの判断規律で、業務で起きた事実を時系列に洗い出し、引き金、担い手、前提、業務上の結果、確からしさを`events`と`actors`として保持する。
4. **scope。** [業務の話の境界](references/boundary.md)、[ドメイン](references/domain.md)、[サブドメイン](references/subdomains.md)、[境界づけられたコンテキスト](references/bounded-contexts.md)と、`write-bdd`の概念の関係にあるdomain-ruleの所有境界を読み、業務の話と実現の関心に線を引き、残ったものをコア・支援・汎用へ理由付きで分ける。該当が無い区分は「なし」と書く。スコープ外へ落とした元の記述、理由、送り先を`implementation_excluded`として保持する。
5. **record-behavior。** [振る舞い発見](references/behavior-discovery.md)、[業務ルール](references/domain-rules.md)、[共通理解を作る問い](references/questions.md)、[成果物の形](references/discovery-deliverable.md)と、`write-bdd`のアクターと利害関係者、ユビキタス言語、BDDの前提・トリガー・失敗理由を適用し、コアの代表的な振る舞い断面、業務の行いの節（誰が行えるか、成り立つ条件、拒む理由）、代表BDD、条件マトリクス、完成本文を同じ文脈で作る。`open_questions`と停止条件で仮説として置いた推奨は、本文の該当箇所に仮説と分かる形で書き、未決の節へ推奨・根拠・採らなかった解釈を並べる。冒頭の段落は、誰が何をなぜ行う業務かを読み手の既知の語で文章として運ぶ。型の読み方の解説やメタ情報の一覧は本文に書かない。
6. **validate（`scripts/scenario_matrix.py`）。** 条件マトリクスJSONを標準入力で`python3 scripts/scenario_matrix.py check`へ渡す。pathはこのSKILLと同じdirectoryを基準にし、fileは介さない。stdoutにJSONを1行ずつ返し、終了codeは0が違反なし、1が違反あり（各行が`path` / `detail` / `howto`）、2が入力を読めない（空、不正JSON、objectでない）。0以外なら資料化へ進まず、診断に従って`record-behavior`へ戻る。

   ```bash
   python3 scripts/scenario_matrix.py check <<'EOF'
   {"scenarios": [{"name": "...", "kind": "success", "expected": "success", "rule": "...", "trigger": {"kind": "action", "text": "..."}, "premises": [{"text": "...", "state": "satisfied", "target": false, "source": "..."}]}]}
   EOF
   ```

7. **document（`write-doc`）。** 完成本文を`{kind: text, content: <完成本文>}`の1要素配列で`material`に、`domain-rule`を`document_type`に、確認済みの`output_directory`と`name`をそのまま渡す。`references`には[成果物の形](references/discovery-deliverable.md)の絶対pathと、入力の`references`を連結して渡す。返った結果の`status`が`completed`で、`path`が指定した保存先と一致することを確かめ、その`path`を`domain_rule_path`にする。`failed`、結果欠落、path不一致なら理由を報告して止まる。

## 停止条件

分からないこと（入力の不足、曖昧な記述、誤りと思われる記述、想定外の値、`grill`で決まらなかった問いと問えなかった論点）に出会ったら、それがこの資料の結論を変えるかどうかで、止まるか進むかを決める。この資料の結論は、扱うコアの範囲、業務イベントとその事前状態、それぞれの行いを誰が行えるか、業務の判断とその後の状態、BDDの結果である。

答えによって結論が変わるなら、推測で埋めずに止まる。資料を保存せず、何が分からないか、それで結論のどこが変わるか、推奨する答えとその根拠を返して判断を求める。誰が行えるか、判断の条件、判断の結果、コアに含めるかどうかが決まらないときが、これに当たる。

結論を変えず、表し方や細部にとどまるなら、最も筋の良い仮説を置いて進む。同じものを指す語のどれを使うか、例に使う値、場面の並べ方が、これに当たる。仮説であること、根拠、採らなかった案を該当箇所と未決に書き、確認は資料とともに求める。どちらの場合も、推測を事実として書かない。

これとは別に、次の場合は止まる。成果が無意味になる必須入力の欠落、公開契約に反する入力、toolの失敗、保存先の不確定である。止まるときは何も書かず、どこまで確定し、何が分かれば続けられるかを返す。

- 題材が読めず、`settle`でも決まらない（何の業務知識を資料にするのかが無い）
- `references`に相対path、読めないpath、symlinkがある
- `grill`が`completed`以外を返した、または`decisions` / `open_questions`が配列でない
- `scenario_matrix.py`が0以外で終了し、`record-behavior`へ戻しても解消しない
- 保存先が確認できない、`output_directory`が無いか相対pathである、同じpathとの衝突を避ける未使用の名前を確認できない、`write-doc`が`completed`以外を返した、または返った`path`が保存先と一致しない。既存資料の存在だけでは停止しない

## 報告

- 保存した資料の絶対pathと、扱った題材
- コア・支援・汎用の線引きと、スコープ外へ落としたものと理由
- コアの代表的な振る舞い断面と、業務の行いの数
- 仮説として置いた事項と、その根拠
- 未確認のまま残った事実と、誰に聞けば確かめられるか
- 停止条件で仮説として置いた論点（推奨つき）
