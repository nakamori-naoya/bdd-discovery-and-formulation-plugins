---
name: discover-user-journey
description: 資料の有無にかかわらず、1人の主たるユーザーが1つの目的を達成するまでを発見し、開始地点、完了条件、接続した場面のBDDを新しい資料として1本作る。「ユーザーJourneyをBDDで発見して」「目的達成までの未知の振る舞いを探索して」と言われたときに使う。指定した既存のJourney資料の反証と同じpathへの更新、ドメインルール、データモデル、テスト実行は扱わない。
---

# ユーザー目的達成BDDの資料を新しく作る

読み終えると、主たるユーザーが目的を達成するまでを、そのユーザーが観測できる場面の連なりとしてBDDの資料1本へ保存できる。この資料は業務の人と開発者が同じ流れを読むためのものであり、ユースケース、UX Journey map、業務ルールの網羅、保存構造、テスト仕様は別の資料が担う。

同じdirectoryの`playbook.yml`が工程順の定義である。このSKILLを読んだagentが、利用者の入力と明示された資料を保持したまま`steps`を宣言順に辿り、最後まで同じ文脈で判断する。`agent_work: invoking_agent`の工程はこのagentの認知工程、`skill:`の工程は同じpackageに同梱された同名skillの`SKILL.md`をこのagentが同じ文脈で読んで適用する工程、`script:`の工程は明示した入力から閉じた結果を返す決定論的tool、`playbook:`の工程は外部公開Skillの直接呼び出しである。工程間の値はこのagentが文脈に保持し、fileへ書き出して受け渡さない。手順に入る前に同梱の内部skill `write-bdd`の`SKILL.md`を同じ文脈で読み、その規律（入力の根拠づけ、業務の言葉、`grill` / `write-doc`の呼び方、BDDの前提・トリガー・失敗理由と条件マトリクス）を全工程へ適用する。

## 入力

| 入力 | 内容 | 満たさないときの扱い |
|---|---|---|
| `user_input` | 依頼文。誰が何を達成したいか、既知の役割、資料の置き場の指定 | 主たるユーザーか目的が読めなければ`settle`の問いにする。決まらなければ止まる |
| `references` | 任意。追加で従う資料の絶対path配列（業務知識、既存Journey、画面案など）。手順の最初に読む。プロジェクト固有の規約や文脈は、対象repositoryのAGENTS.md / CLAUDE.mdとこの入力で渡される | 相対path、読めないpath、symlinkは公開契約に反する入力として止まり、正しいpathを求める |
| `output_directory` | 資料を置く既存の書き込み可能な絶対directory | 下の「保存先の決め方」に従う |
| `name` | path要素を含まない`.md`file名。日本語名を許す | 同上 |

**保存先の決め方。** 依頼に資料構成が示されていれば、そのまま使う。示されていなければ、利用者の既存資料のdirectory階層と命名を読み、主たるユーザーごとに分ける置き場と名前を提案として`settle`の問いの1つに含めて確かめる。資料の置き場は利用者の資料構成に属するので、仮置きで書かない。既定のpathを持たない。`output_directory`が存在しないか相対pathなら止まって利用者へ返す。出力先と同じpathのfileがすでにある場合は上書きせず、未使用の名前を`settle`で提案して確かめる。同じpathへ新規保存できないことを、既存資料があるため探索できないことや、formulationへ切り替える理由にしない。未使用の保存先を確認できれば、既存資料を根拠として探索を続ける。

`write-bdd`の入力に根拠づける規律に従い、利用者の発言、明示された資料、`settle`で確認した決定だけを確定事項にする。

## 判断基準

| 観察対象 | 述語 | 行動 |
|---|---|---|
| 同種または関連する既存資料 | 未知の目的、両端、場面、状態の受け渡しを探索して、新しいJourney資料を作る依頼である | 既存Journey、業務知識、画面案を入力根拠として読み、記載済みというだけで確認済みにせずDiscoveryを続ける。指定された既存のJourney資料を反証し、結果を同じpathへ戻す依頼だけをformulationとする |
| 依頼の問い | 複数の意味ある場面が状態を受け渡し、観測可能な完了へ進む連続性を問うている | Journeyとして扱う。対象システム一つの責任、感情と接点、個別の業務判断、保存事実、操作列を問うているなら非該当理由と適切な成果物を返して止まる |
| 目的を持って始める役割 | 役割×目的の一覧で、今回作る1本が一つに決まる | その1本と残りの順番を`journey_set`として保持する。決まらなければ今回の1本という結論が決まらないので、停止条件に従って止まり、候補と推奨を返す |
| 場面や分岐の候補 | この違いが起きたとき、主たるユーザーが何を見て気づく | 書く。気づかないなら書かず、書かないことへの断り書きも書かない |
| `When`に置く役割 | 主たるユーザー自身、または主たるユーザーとやり取りする人である | 書く。通知基盤や集計処理のようなシステム上の主体は、主たるユーザーが観測する結果へ畳む |
| `Then`と完了条件 | 主たるユーザーが見る、知らされる、判断できる、次にできるようになる、のどれかで書けている | 書く。期間の開始、対象化、保存状態のような内部の成立だけなら、確認済みの時間境界を保ったまま利用者が観測する結果へ接続する。接続できなければ最も筋の良い観測結果を仮説として書き、未決にする |
| 語彙 | 入力根拠にある利用者の言葉である | 使う。内部モデルの造語や未合意の呼び名は作らず、入力にある語を仮置きして未決にする |
| 成功に必要な前提 | 値ごと`Given`に書けている（境界値も含む） | 書く。業務知識の資料にあるという理由で省かない |
| 同じ主たるユーザー・同じ目的で条件だけが違う流れ | 同じJourneyの分岐として書ける | 1本の中の分岐にする。目的が違うなら別のJourneyとして`journey_set`の次候補へ置く |
| 前の場面の結果と次の場面の前提 | 前者から後者が成立する理由を説明できる | 接続として書く。説明できなければ、最も筋の良い接続を仮説として書き、仮説であることと根拠を該当箇所と未決に明示する |
| Journeyから外した事項 | 業務知識、非機能要件、別Journey、実現方式のどれに送るか決まる | 送り先を作業記録に残す |

場面数と資料の長さに上限を置かない。目的へ寄与しない場面、同じ結果を繰り返す場面、前後が接続しない場面だけを問題にする。

## 手順

1. **settle（`grill`）。** [実行指示書](references/execution-guidance.md)の「問いの選び方」で、答えによって資料の骨格（主たるユーザー、目的、両端、完了条件、場面の接続）が変わる問いを成果を左右する順に選び、推奨と理由を添えて`context`と`questions`に渡す。最初の問いは「目的を持って始める役割はどれとどれか、各役割の目的は何か」である。保存先が依頼に無ければ、その提案を問いに含める。呼び方は`write-bdd`の入れ子の段取りを呼ぶ規律に従う。問う数の上限と対話の作法はgrillの公開契約に従い、返った`open_questions`と、上限で問えなかった論点は、停止条件の判断基準で扱う。返った`decisions`と`open_questions`を利用者入力・明示資料と突き合わせる。2回目の`grill`は、利用者が求めた場合か、決定なしでは資料を完成できない場合だけ行う。
2. **ground。** 依頼、参照資料、決定、未決、停止条件で仮説として置いた推奨を根拠・仮説・未確認へ区別して`grounded_input`として保持する。
3. **select-journey。** 役割×目的の一覧を作り、今回作る1本と残りの順番を`journey_set`として決める。
4. **map-journey。** 同梱skillの判断規律で、Journeyに該当するかを判定し、主たるユーザー、目的、開始地点、最終地点、完了条件、場面、状態の受け渡し、既知の分岐を`journey_map`として同じ文脈に保持する。
5. **compose。** `map-journey`で適用した同梱skillのJourneyを判定し接続する判断規律、Journeyの構造、場面のBDDと、`write-bdd`のBDDの前提・トリガー・失敗理由を適用し、場面ごとの`Given / When / Then`、条件マトリクス、完成本文を作る。`open_questions`と停止条件で仮説として置いた推奨は、該当する場面に仮説と分かる形で書き、未決の節へ推奨・根拠・採らなかった解釈を並べる。冒頭の段落は、誰が何を達成するために、どこから始まり、何が観測できたら完了かを読み手の既知の語で文章として運ぶ。型の読み方の解説やメタ情報の一覧は本文に書かない。
6. **validate（`scripts/scenario.py`）。** 場面草案（`user-journey-bdd`型の本文記法: `## 場面 <連番>: <名前>`、コードフェンス内の`Given:` / `And:` / `When:` / `Then:`、失敗場面の`NOTE: Rule:` / `Source:` / `Reason:`、`**接続**:`）をそのまま標準入力で、条件マトリクスを`--matrix-json`引数のJSON文字列で`python3 scripts/scenario.py check`へ渡す。pathはこのSKILLと同じdirectoryを基準にし、fileは介さない。stdoutにJSONを1行ずつ返し、終了codeは0が違反なし、1が違反あり（各行が`line` / `kind` / `detail` / `howto`）、2が入力を読めない（標準入力が空、`--matrix-json`がJSONでないかobjectでない）。0以外なら資料化へ進まず、診断に従って`compose`へ戻る。

   ````bash
   python3 scripts/scenario.py check --matrix-json '{"scenarios": [{"name": "希望を伝える", "kind": "success", "expected": "success", "rule": "予約成立規則", "trigger": {"kind": "action", "text": "予約者が希望を伝える"}, "premises": [{"text": "予約者が希望条件を決めている", "state": "satisfied", "target": false, "source": "予約資料"}]}]}' <<'EOF'
   # 予約を完了する

   予約者が希望する条件で予約を成立させるまでを扱う。

   ## 場面 1: 希望を伝える

   ```gherkin
   Given: 予約者が希望条件を決めている
   When: 予約者が希望を伝える
   Then: 希望に合う候補が示される
   ```
   EOF
   ````

7. **document（`write-doc`）。** 完成本文を`{kind: text, content: <完成本文>}`の1要素配列で`material`に、`user-journey-bdd`を`document_type`に、確認済みの`output_directory`と`name`をそのまま渡す。`references`には入力の`references`をそのまま渡す（空なら空配列）。返った結果の`status`が`completed`で、`path`が指定した保存先と一致することを確かめ、その`path`を`user_journey_bdd_path`にする。`failed`、結果欠落、path不一致なら理由を報告して止まる。

## 停止条件

分からないこと（入力の不足、曖昧な記述、誤りと思われる記述、想定外の値、`grill`で決まらなかった問いと問えなかった論点）に出会ったら、それがこの資料の結論を変えるかどうかで、止まるか進むかを決める。この資料の結論は、主たるユーザーと目的、開始地点と完了条件、場面の並びと接続と分岐、各場面で主たるユーザーが観測する結果である。

答えによって結論が変わるなら、推測で埋めずに止まる。資料を保存せず、何が分からないか、それで結論のどこが変わるか、推奨する答えとその根拠を返して判断を求める。開始地点、完了条件、場面の接続や分岐が決まらないときが、これに当たる。

結論を変えず、表し方や細部にとどまるなら、最も筋の良い仮説を置いて進む。前提に置く例の値、場面の呼び名が、これに当たる。仮説であること、根拠、採らなかった案を該当箇所と未決に書き、確認は資料とともに求める。どちらの場合も、推測を事実として書かない。

これとは別に、次の場合は止まる。成果が無意味になる必須入力の欠落、公開契約に反する入力、toolの失敗、保存先の不確定である。止まるときは何も書かず、どこまで確定し、何が分かれば続けられるかを返す。

- 主たるユーザーと目的が入力からも`settle`からも決まらない（誰の何の達成を書くのかが無い）
- 依頼がJourneyの問いではない。非該当理由と適切な成果物（ユースケース、UX Journey map、業務知識、データモデル）を返す
- `references`に相対path、読めないpath、symlinkがある
- `grill`が`completed`以外を返した、または`decisions` / `open_questions`が配列でない
- `scenario.py`が0以外で終了し、`compose`へ戻しても解消しない
- 保存先が確認できない、`output_directory`が無いか相対pathである、同じpathとの衝突を避ける未使用の名前を確認できない、`write-doc`が`completed`以外を返した、または返った`path`が保存先と一致しない。既存資料の存在だけでは停止しない

既存のdomain-ruleやdata model資料は入力根拠として読むが変更せず、そこに不足が見つかれば未決として送り先を記録する。

## 報告

- 保存した資料の絶対path
- 主たるユーザー、目的、開始地点、最終地点、完了条件、場面数と主要な接続
- `journey_set`（役割×目的の一覧、今回作った1本、残りの順番）。資料には書かない作業記録である
- 仮説として置いた事項と、その根拠
- Journeyから外した事項と送り先（業務知識 / 非機能要件 / 別Journey / 実現方式）
- 未決の接続と確認すべき人、停止条件で仮説として置いた論点（推奨つき）
