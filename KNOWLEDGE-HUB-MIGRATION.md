# knowledge-hubへのBDDハーネス移行方針

## この資料の目的

この資料は、BDDのドメイン理解とデータモデリングに使うハーネスを、なぜknowledge-hubへ移行するのか、移行によって何を実現したいのかを明確にする。

配置場所、directory構成、コピー手順、install commandを決める資料ではない。移行時に判断がぶれないよう、目的と対象範囲を合意するための資料である。

## 要旨

knowledge-hubへ移行したいのは、既存の大きなplugin群をそのまま複製するためではない。

BDDのdiscoveryとformulationにおいて、利用者が提示していない用語・イベント・概念をAIが補ってしまうことを防ぎ、不明点は利用者との対話で確かめ、確認済みの内容だけからドメイン理解とデータモデルを作る。そのために必要なplaybook、skill、共有資料だけを、knowledge-hub側で継続的に調整できる正本として持つことが目的である。

## WHY — なぜ移行するのか

### 1. AIによる「もっともらしい補完」を防ぐ

BDDのdiscoveryやformulationでは、入力にない用語、イベント、役割、状態、制約をAIが補うと、読みやすくても根拠のない資料ができる。特にドメイン理解とデータモデリングでは、その補完が後続のシナリオ、テーブル、ルールへ連鎖する。

移行後は、次の情報だけを確定事項の根拠にする。

- 利用者が入力した内容
- 利用者が明示的に参照させた資料
- 対話によって利用者が確認した決定

不足している情報を一般知識や例示から埋めるのではなく、未決として扱えることを重視する。

### 2. 不明点を対話で解消する

不明点や深掘りが必要な点があるときは、後続工程で推測するのではなく、最初に`grill`で利用者へ確認する。一度に質問を並べるのではなく、一問ずつ問い、回答を受け取ってから次へ進む。

確認が終わるまでは、ドメインイベントの抽出、BDDの作成、データモデル設計、資料化へ進まない。これにより、対話で確認した内容と成果物の間を追跡できるようにする。

### 3. knowledge-hubで調整できる正本を持つ

install済みcacheを直接変更すると、再installやversion更新で変更が失われ、誰が何を意図して変えたのかも残りにくい。

knowledge-hub側にsourceを持ち、skillの表現、質問の進め方、判断資料、playbookの契約をversion管理する。チーム固有の調整はこの正本へ加え、検証してから配布する状態を作りたい。

### 4. 必要な関心だけに絞る

今回必要なのは、BDDによるドメイン理解とデータモデリングである。Slackやmeetingからの収集、digest、定期実行、agent起動、pull request運用などは、この目的の実行時依存ではない。

関係のない機能を一緒に持ち込まないことで、利用者が選ぶ入口、調整対象、検証範囲を明確にする。

## WHAT — 何を移行対象にするのか

### 利用者に提供する4つの入口

| 入口 | 実現したいこと |
|---|---|
| `domain-bdd-discovery` | 利用者との対話からコアドメインの共通理解を作り、代表BDDを含む資料にする |
| `domain-bdd-formulation` | 既存のドメイン資料を入力にし、確認済みの理解をQA観点で反証して同じ資料を深化させる |
| `data-model-bdd-discovery` | 業務イベントと永続化の振る舞いを確認し、BDDを含むRDB論理データモデル資料にする |
| `data-model-bdd-formulation` | 既存のBDD付き論理設計を反証して深化させ、論理構造を保ったままRDB物理設計へつなぐ |

### 4つの入口に共通する契約

- 最初の工程は`grill`とする。
- 不明点や深掘りが必要な点は一問ずつ確認する。
- 回答を待っている間は後続工程へ進まない。
- 確認済みの回答だけを`grounded_input`として後続へ渡す。
- 利用者入力、明示された資料、対話で確認した決定にない用語・イベント・概念を作らない。
- 成果物はMarkdownとして扱う。

### 入口を成立させる機能

4つの入口だけを置いても、実際の処理は完結しない。移行対象には、入口から直接または間接に使われる次の機能を含める。

- 不明点を対話で確認する機能
- 業務イベントを探索する機能
- コアドメインを見極める機能
- 永続化シナリオを作る機能
- 論理データモデルを設計する機能
- RDB物理設計を作る機能
- 資料の型を選び、執筆規律を適用し、保存する機能
- 各skillとplaybookが共通して使う設定解決、入力根拠、ドメイン判断、データモデリング判断、BDD品質の資料

資料型は、今回の目的に必要な次の2種類だけを扱う。

- `domain-rule`
- `rdb-logical-data-modeling`

### 移行対象にしないもの

次は今回のWHYを実現するために必要ではないため、移行対象にしない。

- Slack、meeting、sessionの収集
- 日次・週次・月次などのdigest
- 定期実行
- 別agentの起動やagent運用
- pull requestの作成、review、merge支援
- use-case BDDとjourney BDD
- MCP server、connector、hook
- 上記の機能だけが利用する設定、script、共有資料

## 移行後に実現したい状態

### knowledge-hub側が正本である

BDDのドメイン理解とデータモデリングに関する調整は、knowledge-hub側のsourceへ行う。install済みcacheは実行結果であり、編集対象にしない。

### 意図した変更だけを配布できる

skillの一つの表現を変えたい場合、その変更をsource、version、検証結果とともに残す。上流の更新を取り込む場合も、上流からのコピーとknowledge-hub固有の変更を区別できるようにする。

### 入力根拠の契約を機械的に壊せない

次のような変更は検証で失敗させる。

- `grill`を先頭から外す
- `grill`を省く
- 未確認の情報を後続へ渡せるようにする
- `grounded_input`を後続工程へ渡さない
- 必要なplugin、skill、scriptを欠落させる
- 対象外のpluginや資料型を追加する

### 複数のagentから同じ意図で利用できる

Claude CodeとCodexで発見方法やpackage manifestは異なっても、実際に読むskill、playbook、script、判断資料は同じ内容を使う。agentごとに別のBDD手順へ分岐させない。

## 移行の完了条件

移行は、ファイルをコピーした時点では完了としない。次の状態を満たしたときに完了とする。

- 4つの入口と必要な依存だけが配布対象になっている。
- 両agentが同じskillとplaybookを利用できる。
- 4入口すべてで`grill`と`grounded_input`の契約が有効である。
- 正常系だけでなく、契約を意図的に壊した負の試験も通っている。
- 電子チケットのお題のような未整理入力に対して、ヒントを補わず、必要な確認から始められる。
- 上流由来の内容とknowledge-hub固有の調整を区別できる。
- 調整、検証、version更新、配布をsourceから繰り返せる。

## 今回決めないこと

次はWHYとWHATを変えないため、この資料では決めない。

- knowledge-hub内の配置場所
- directory名
- 正式なmarketplace名
- 最終的なplugin名
- releaseの運用方法

これらを後から決める場合も、「入力にない概念を作らない」「不明点は対話で確認する」「ドメインBDDとデータモデリングBDDだけに絞る」という目的を変えない。

## 分離済みmarketplaceとの関係

この方針の実現可能性は、分離済みのBDD marketplaceで確認している。コピー境界と上流差分は[VENDORING.md](VENDORING.md)、実行する検証は[VALIDATION.md](VALIDATION.md)を参照する。
